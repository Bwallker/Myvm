{
  "scripts": {
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && webpack",
    "serve": "export NODE_OPTIONS=--openssl-legacy-provider && webpack-dev-server"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "1.0.1",
    "html-webpack-plugin": "^3.2.0",
    "text-encoding": "^0.7.0",
    "webpack": "^4.46.0",
    "webpack-cli": "^3.1.1",
    "webpack-dev-server": "^3.1.0"
  }
}
