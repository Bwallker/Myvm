[package]
name = "wasm_interface"
version = "0.1.0"
authors = ["Bwallker"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2.80"
assembler = { path = "../assembler" }